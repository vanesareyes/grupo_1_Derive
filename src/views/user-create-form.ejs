<!DOCTYPE html>
<html lang="en">

   <%- include partials/head %>
<body>

  <!-- NavBar -->
  <%- include partials/usernavbar %>

<!-- Register Content -->
<% if (typeof errors != 'undefined') { %>
   <p style="color:red">ERROR</p>
   <ul>
      <% for (let i =0; i < errors.length; i++){ %>
         <li>
            <%=errors[i].msg%>
         </li>
      <% } %>
   </ul>
<% } %>
<div class="container pt-5">
  <div class="row">
    <div class="col-lg-6">
        <div class="header-title">
            <h1 class="wv-heading--title">
               Gracias por registrarte!
            </h1>
            <h2 class="wv-heading--subtitle">
               Completar tu perfil nos ayuda a brindarte una experiencia personalizada, a la medida de tus gustos y búsquedas.
            </h2>
           </div>
    </div>
    <div class="col-lg-6">
        <div class="myform form ">
            <form action="" method="post" name="register">
             <div class="form-group">
                <input type="text" name="name"  class="form-control my-input" id="name" placeholder="Nombre" value="<%= locals.data.name ? locals.data.name : '' %>">
             </div>
             <div class="form-group">
                 <input type="text" name="surname"  class="form-control my-input" id="surname" placeholder="Apellido" value="<%= locals.data.surname ? locals.data.surname : '' %>">
              </div>
             <div class="form-group">
              <input type="email" name="email"  class="form-control my-input" id="email" placeholder="eMail" value="<%= locals.data.email ? locals.data.email : '' %>">
             </div>
             <div class="form-group">
              <input type="number" min="0" name="phone" id="phone"  class="form-control my-input" placeholder="Teléfono" value="<%= locals.data.phone ? locals.data.phone : '' %>">
             </div>
             
             <!--<div class="form-group">
               <input type="password" min="0" name="password" id="password"  class="form-control my-input" placeholder="Contraseña">
              </div>-->

              <div class="form-group">
                <label for="UserImageFile">Cargar imagen de perfil</label>
                <input type="file" class="form-control-file" id="UserImageFile">
              </div>

             <div class="text-center ">
              <button type="submit" class=" btn btn-block send-button tx-tfm">Cargar mis datos!</button>
             </div>


             <div class="col-md-12 ">
              <div class="login-or">
               <hr class="hr-or">
               <!--<span class="span-or">o</span>-->
              </div>
             </div>
             <!--<div class="form-group">
              <a class="btn btn-block g-button" href="#">
                 <i class="fab fa-google"></i> • Ingresar con Google
              </a>
             </div>-->
             <p class="small mt-3">Al registrarte, nos indicas que has leído y aceptado nuestros <a href="#" class="ps-hero__content__link">Términos y Condiciones de Uso</a> y nuestra <a href="#">Política de Privacidad</a>.
             </p>
            </form>
         </div>
    </div>
  </div>
</div>

  <!-- Footer -->
  <%- include partials/footer.ejs %>

</body>

</html>
